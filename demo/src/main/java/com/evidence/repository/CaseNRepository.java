package com.evidence.repository;

import com.evidence.model.CaseN;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CaseNRepository extends JpaRepository<CaseN, Integer> {
}